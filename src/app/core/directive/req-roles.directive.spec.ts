import { ReqRolesDirective } from './req-roles.directive';

describe('ReqRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ReqRolesDirective();
    expect(directive).toBeTruthy();
  });
});
