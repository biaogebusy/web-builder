import { Meta } from '@storybook/blocks';

<Meta title="部署/部署流程" id="workflow" />

## Web builder 完整的部署流程

1. 部署 xinshi cms 到服务器，配置后台域名和Drupal的相关配置；
2. 部署 web builder 到服务器，配置前台域名；
3. 访问web builder，配置builder，如果是导入web builder数据库初始化的站点可忽略；
