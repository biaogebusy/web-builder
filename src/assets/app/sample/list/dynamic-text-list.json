{
  "title": "动态文字列表",
  "body": [{
    "type": "dynamic-text-list",
    "params": {
      "type": "question",
      "options": "fields[node--question]=title,changed,path,drupal_internal__nid,answer,uid&include=uid,uid.user_picture&fields[user--user]=name,user_picture&fields[file--file]=uri&jsonapi_include=1",
      "sort": "-changed",
      "limit": "20"
    },
    "action": {
      "icon": "editor",
      "label": "写回答"
    }
  }]
}
